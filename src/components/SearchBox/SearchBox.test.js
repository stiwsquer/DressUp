import SearchBox from "./SearchBox";
import { SearchBoxWrapper } from "./SearchBox.style";

let shallowWrapper;
let mountWrapper;
let renderWrapper;
beforeAll(() => {
  // shallowWrapper = shallow(<SearchBox />);
  // mountWrapper = mount(<SearchBox />);
  // renderWrapper = render(<SearchBox />);
});

it("renders SearchBox component without crashing", () => {
  // shallow(<SearchBox />);
});

// it("contains two SearchBoxWrapper classes", () => {
//   expect(shallowWrapper.find(SearchBoxWrapper)).toHaveLength(1);
// });

// it("contains Input component", () => {
//   expect(shallowWrapper.find(Input)).toHaveLength(1);
// });
